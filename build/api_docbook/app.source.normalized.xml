<?xml version="1.0" encoding="UTF-8"?>
<file>
   <object title="app">
      <methods>
         <method name="quit">
            <description>
               <para>Try to close all windows. The before-quit event will be emitted first. If all windows are successfully closed, the will-quit event will be emitted and by default the application will terminate.</para>
               <para>This method guarantees that all beforeunload and unload event handlers are correctly executed. It is possible that a window cancels the quitting by returning false in the beforeunload event handler.</para>
            </description>
         </method>
         <method name="exit">
            <param optional="true" name="exitCode" type="Integer"/>
            <description>
               <para>Exits immediately with exitCode. exitCode defaults to 0.</para>
               <para>All windows will be closed immediately without asking user and the before-quit and will-quit events will not be emitted.</para>
            </description>
         </method>
         <method name="relaunch">
            <param optional="true" name="options" type="Object">
               <property optional="true" name="args" type="String" isArray="true"/>
               <property optional="true" name="execPath" type="String"/>
            </param>
            <description>
               <para>Relaunches the app when current instance exits.</para>
               <para>By default the new instance will use the same working directory and command line arguments with current instance. When args is specified, the args will be passed as command line arguments instead. When execPath is specified, the execPath will be executed for relaunch instead of current app.</para>
               <para>Note that this method does not quit the app when executed, you have to call app.quit or app.exit after calling app.relaunch to make the app restart.</para>
               <para>When app.relaunch is called for multiple times, multiple instances will be started after current instance exited.</para>
               <para>An example of restarting current instance immediately and adding a new command line argument to the new instance:</para>
               <programlisting>...omitted...</programlisting>
            </description>
         </method>
         <method name="isReady">
            <returns type="Boolean">
               <description>
                  <para>- true if Electron has finished initializing, false otherwise.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="focus">
            <description>
               <para>On Linux, focuses on the first visible window. On macOS, makes the application the active app. On Windows, focuses on the application's first window.</para>
            </description>
         </method>
         <method name="hide">
            <description>
               <para>Hides all application windows without minimizing them.</para>
            </description>
         </method>
         <method name="show">
            <description>
               <para>Shows application windows after they were hidden. Does not automatically focus them.</para>
            </description>
         </method>
         <method name="getAppPath">
            <returns type="String">
               <description>
                  <para>- The current application directory.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="getPath">
            <param name="name" type="String"/>
            <returns type="String">
               <description>
                  <para>- A path to a special directory or file associated with name. On failure an Error is thrown.</para>
               </description>
            </returns>
            <description>
               <para>You can request the following paths by the name:</para>
               <list>
                  <item>
                     <para>home User's home directory.</para>
                  </item>
                  <item>
                     <para>appData Per-user application data directory, which by default points to:</para>
                     <list>
                        <item>
                           <para>%APPDATA% on Windows</para>
                        </item>
                        <item>
                           <para>$XDG_CONFIG_HOME or ~/.config on Linux</para>
                        </item>
                        <item>
                           <para>~/Library/Application Support on macOS</para>
                        </item>
                     </list>
                  </item>
                  <item>
                     <para>userData The directory for storing your app's configuration files, which by default it is the appData directory appended with your app's name.</para>
                  </item>
                  <item>
                     <para>temp Temporary directory.</para>
                  </item>
                  <item>
                     <para>exe The current executable file.</para>
                  </item>
                  <item>
                     <para>module The libchromiumcontent library.</para>
                  </item>
                  <item>
                     <para>desktop The current user's Desktop directory.</para>
                  </item>
                  <item>
                     <para>documents Directory for a user's "My Documents".</para>
                  </item>
                  <item>
                     <para>downloads Directory for a user's downloads.</para>
                  </item>
                  <item>
                     <para>music Directory for a user's music.</para>
                  </item>
                  <item>
                     <para>pictures Directory for a user's pictures.</para>
                  </item>
                  <item>
                     <para>videos Directory for a user's videos.</para>
                  </item>
                  <item>
                     <para>pepperFlashSystemPlugin Full path to the system version of the Pepper Flash plugin.</para>
                  </item>
               </list>
            </description>
         </method>
         <method name="getFileIcon">
            <param name="path" type="String"/>
            <param optional="true" name="options" type="Object">
               <property name="size" type="String">
                  <param name="small"/>
                  <param name="normal"/>
                  <param name="large"/>
               </property>
            </param>
            <param name="callback" type="Function">
               <param name="error" type="Error"/>
               <param name="icon" type="NativeImage"/>
            </param>
            <description>
               <para>Fetches a path's associated icon.</para>
               <para>On Windows, there a 2 kinds of icons:</para>
               <list>
                  <item>
                     <para>Icons associated with certain file extensions, like .mp3, .png, etc.</para>
                  </item>
                  <item>
                     <para>Icons inside the file itself, like .exe, .dll, .ico.</para>
                  </item>
               </list>
               <para>On Linux and macOS, icons depend on the application associated with file mime type.</para>
            </description>
         </method>
         <method name="setPath">
            <param name="name" type="String"/>
            <param name="path" type="String"/>
            <description>
               <para>Overrides the path to a special directory or file associated with name. If the path specifies a directory that does not exist, the directory will be created by this method. On failure an Error is thrown.</para>
               <para>You can only override paths of a name defined in app.getPath.</para>
               <para>By default, web pages' cookies and caches will be stored under the userData directory. If you want to change this location, you have to override the userData path before the ready event of the app module is emitted.</para>
            </description>
         </method>
         <method name="getVersion">
            <returns type="String">
               <description>
                  <para>- The version of the loaded application. If no version is found in the application's package.json file, the version of the current bundle or executable is returned.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="getName">
            <returns type="String">
               <description>
                  <para>- The current application's name, which is the name in the application's package.json file.</para>
               </description>
            </returns>
            <description>
               <para>Usually the name field of package.json is a short lowercased name, according to the npm modules spec. You should usually also specify a productName field, which is your application's full capitalized name, and which will be preferred over name by Electron.</para>
            </description>
         </method>
         <method name="setName">
            <param name="name" type="String"/>
            <description>
               <para>Overrides the current application's name.</para>
            </description>
         </method>
         <method name="getLocale">
            <returns type="String">
               <description>
                  <para>- The current application locale. Possible return values are documented here.</para>
               </description>
            </returns>
            <description>
               <para>Note: When distributing your packaged app, you have to also ship the locales folder.</para>
               <para>Note: On Windows you have to call it after the ready events gets emitted.</para>
            </description>
         </method>
         <method name="addRecentDocument">
            <param name="path" type="String"/>
            <description>
               <para>Adds path to the recent documents list.</para>
               <para>This list is managed by the OS. On Windows you can visit the list from the task bar, and on macOS you can visit it from dock menu.</para>
            </description>
         </method>
         <method name="clearRecentDocuments">
            <description>
               <para>Clears the recent documents list.</para>
            </description>
         </method>
         <method name="setAsDefaultProtocolClient">
            <param name="protocol" type="String"/>
            <param optional="true" name="path" type="String"/>
            <param optional="true" name="args" type="String" isArray="true"/>
            <returns type="Boolean">
               <description>
                  <para>- Whether the call succeeded.</para>
               </description>
            </returns>
            <description>
               <para>This method sets the current executable as the default handler for a protocol (aka URI scheme). It allows you to integrate your app deeper into the operating system. Once registered, all links with your-protocol:// will be opened with the current executable. The whole link, including protocol, will be passed to your application as a parameter.</para>
               <para>On Windows you can provide optional parameters path, the path to your executable, and args, an array of arguments to be passed to your executable when it launches.</para>
               <para>Note: On macOS, you can only register protocols that have been added to your app's info.plist, which can not be modified at runtime. You can however change the file with a simple text editor or script during build time. Please refer to Apple's documentation for details.</para>
               <para>The API uses the Windows Registry and LSSetDefaultHandlerForURLScheme internally.</para>
            </description>
         </method>
         <method name="removeAsDefaultProtocolClient">
            <param name="protocol" type="String"/>
            <param optional="true" name="path" type="String"/>
            <param optional="true" name="args" type="String" isArray="true"/>
            <returns type="Boolean">
               <description>
                  <para>- Whether the call succeeded.</para>
               </description>
            </returns>
            <description>
               <para>This method checks if the current executable as the default handler for a protocol (aka URI scheme). If so, it will remove the app as the default handler.</para>
            </description>
         </method>
         <method name="isDefaultProtocolClient">
            <param name="protocol" type="String"/>
            <param optional="true" name="path" type="String"/>
            <param optional="true" name="args" type="String" isArray="true"/>
            <returns type="Boolean">
               <description>
                  <para/>
               </description>
            </returns>
            <description>
               <para>This method checks if the current executable is the default handler for a protocol (aka URI scheme). If so, it will return true. Otherwise, it will return false.</para>
               <para>Note: On macOS, you can use this method to check if the app has been registered as the default protocol handler for a protocol. You can also verify this by checking ~/Library/Preferences/com.apple.LaunchServices.plist on the macOS machine. Please refer to Apple's documentation for details.</para>
               <para>The API uses the Windows Registry and LSCopyDefaultHandlerForURLScheme internally.</para>
            </description>
         </method>
         <method name="setUserTasks">
            <param name="tasks" type="Task" isArray="true"/>
            <returns type="Boolean">
               <description>
                  <para>- Whether the call succeeded.</para>
               </description>
            </returns>
            <description>
               <para>Adds tasks to the Tasks category of the JumpList on Windows.</para>
               <para>tasks is an array of Task objects.</para>
               <para>Note: If you'd like to customize the Jump List even more use app.setJumpList(categories) instead.</para>
            </description>
         </method>
         <method name="getJumpListSettings">
            <returns type="Object">
               <param name="minItems" type="Integer"/>
               <param name="removedItems" type="JumpListItem" isArray="true"/>
               <description>
                  <para>:</para>
               </description>
            </returns>
            <param name="minItems" type="Integer"/>
            <param name="removedItems" type="JumpListItem" isArray="true"/>
            <description>
               <list>
                  <item>
                     <para>minItems Integer - The minimum number of items that will be shown in the Jump List (for a more detailed description of this value see the MSDN docs).</para>
                  </item>
                  <item>
                     <para>removedItemsJumpListItem[] - Array of JumpListItem objects that correspond to items that the user has explicitly removed from custom categories in the Jump List. These items must not be re-added to the Jump List in the next call to app.setJumpList(), Windows will not display any custom category that contains any of the removed items.</para>
                  </item>
               </list>
            </description>
         </method>
         <method name="setJumpList">
            <param name="categories" type="JumpListCategory" isArray="true"/>
            <description>
               <para>Sets or removes a custom Jump List for the application, and returns one of the following strings:</para>
               <list>
                  <item>
                     <para>ok - Nothing went wrong.</para>
                  </item>
                  <item>
                     <para>error - One or more errors occurred, enable runtime logging to figure out the likely cause.</para>
                  </item>
                  <item>
                     <para>invalidSeparatorError - An attempt was made to add a separator to a custom category in the Jump List. Separators are only allowed in the standard Tasks category.</para>
                  </item>
                  <item>
                     <para>fileTypeRegistrationError - An attempt was made to add a file link to the Jump List for a file type the app isn't registered to handle.</para>
                  </item>
                  <item>
                     <para>customCategoryAccessDeniedError - Custom categories can't be added to the Jump List due to user privacy or group policy settings.</para>
                  </item>
               </list>
               <para>If categories is null the previously set custom Jump List (if any) will be replaced by the standard Jump List for the app (managed by Windows).</para>
               <para>Note: If a JumpListCategory object has neither the type nor the name property set then its type is assumed to be tasks. If the name property is set but the type property is omitted then the type is assumed to be custom.</para>
               <para>Note: Users can remove items from custom categories, and Windows will not allow a removed item to be added back into a custom category until after the next successful call to app.setJumpList(categories). Any attempt to re-add a removed item to a custom category earlier than that will result in the entire custom category being omitted from the Jump List. The list of removed items can be obtained using app.getJumpListSettings().</para>
               <para>Here's a very simple example of creating a custom Jump List:</para>
               <programlisting>...omitted...</programlisting>
            </description>
         </method>
         <method name="makeSingleInstance">
            <param name="callback" type="Function">
               <param name="argv" type="String" isArray="true"/>
               <param name="workingDirectory" type="String"/>
            </param>
            <description>
               <para>This method makes your application a Single Instance Application - instead of allowing multiple instances of your app to run, this will ensure that only a single instance of your app is running, and other instances signal this instance and exit.</para>
               <para>callback will be called with callback(argv, workingDirectory) when a second instance has been executed. argv is an Array of the second instance's command line arguments, and workingDirectory is its current working directory. Usually applications respond to this by making their primary window focused and non-minimized.</para>
               <para>The callback is guaranteed to be executed after the ready event of app gets emitted.</para>
               <para>This method returns false if your process is the primary instance of the application and your app should continue loading. And returns true if your process has sent its parameters to another instance, and you should immediately quit.</para>
               <para>On macOS the system enforces single instance automatically when users try to open a second instance of your app in Finder, and the open-file and open-url events will be emitted for that. However when users start your app in command line the system's single instance mechanism will be bypassed and you have to use this method to ensure single instance.</para>
               <para>An example of activating the window of primary instance when a second instance starts:</para>
               <programlisting>...omitted...</programlisting>
            </description>
         </method>
         <method name="releaseSingleInstance">
            <description>
               <para>Releases all locks that were created by makeSingleInstance. This will allow multiple instances of the application to once again run side by side.</para>
            </description>
         </method>
         <method name="setUserActivity">
            <param name="type" type="String"/>
            <param name="userInfo" type="Object"/>
            <param optional="true" name="webpageURL" type="String"/>
            <description>
               <para>Creates an NSUserActivity and sets it as the current activity. The activity is eligible for Handoff to another device afterward.</para>
            </description>
         </method>
         <method name="getCurrentActivityType">
            <returns type="String">
               <description>
                  <para>- The type of the currently running activity.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="setAppUserModelId">
            <param name="id" type="String"/>
            <description>
               <para>Changes the Application User Model ID to id.</para>
            </description>
         </method>
         <method name="importCertificate">
            <param name="options" type="Object">
               <property name="certificate" type="String"/>
               <property name="password" type="String"/>
            </param>
            <param name="callback" type="Function">
               <param name="result" type="Integer"/>
            </param>
            <description>
               <para>Imports the certificate in pkcs12 format into the platform certificate store. callback is called with the result of import operation, a value of 0 indicates success while any other value indicates failure according to chromium net_error_list.</para>
            </description>
         </method>
         <method name="disableHardwareAcceleration">
            <description>
               <para>Disables hardware acceleration for current app.</para>
               <para>This method can only be called before app is ready.</para>
            </description>
         </method>
         <method name="setBadgeCount">
            <param name="count" type="Integer"/>
            <returns type="Boolean">
               <description>
                  <para>- Whether the call succeeded.</para>
               </description>
            </returns>
            <description>
               <para>Sets the counter badge for current app. Setting the count to 0 will hide the badge.</para>
               <para>On macOS it shows on the dock icon. On Linux it only works for Unity launcher,</para>
               <para>Note: Unity launcher requires the existence of a .desktop file to work, for more information please read Desktop Environment Integration.</para>
            </description>
         </method>
         <method name="getBadgeCount">
            <returns type="Integer">
               <description>
                  <para>- The current value displayed in the counter badge.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="isUnityRunning">
            <returns type="Boolean">
               <description>
                  <para>- Whether the current desktop environment is Unity launcher.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="getLoginItemSettings">
            <param optional="true" name="options" type="Object">
               <property optional="true" name="path" type="String"/>
               <property optional="true" name="args" type="String" isArray="true"/>
            </param>
            <returns type="Object">
               <itemizedlist spacing="compact">
                  <param name="openAtLogin" type="Boolean"/>
                  <param name="openAsHidden" type="Boolean"/>
                  <param name="wasOpenedAtLogin" type="Boolean"/>
                  <param name="wasOpenedAsHidden" type="Boolean"/>
                  <param name="restoreState" type="Boolean"/>
               </itemizedlist>
               <description>
                  <para>:</para>
               </description>
            </returns>
            <description>
               <para>If you provided path and args options to app.setLoginItemSettings then you need to pass the same arguments here for openAtLogin to be set correctly.</para>
               <list>
                  <item>
                     <para>openAtLogin Boolean - true if the app is set to open at login.</para>
                  </item>
                  <item>
                     <para>openAsHidden Boolean - true if the app is set to open as hidden at login. This setting is only supported on macOS.</para>
                  </item>
                  <item>
                     <para>wasOpenedAtLogin Boolean - true if the app was opened at login automatically. This setting is only supported on macOS.</para>
                  </item>
                  <item>
                     <para>wasOpenedAsHidden Boolean - true if the app was opened as a hidden login item. This indicates that the app should not open any windows at startup. This setting is only supported on macOS.</para>
                  </item>
                  <item>
                     <para>restoreState Boolean - true if the app was opened as a login item that should restore the state from the previous session. This indicates that the app should restore the windows that were open the last time the app was closed. This setting is only supported on macOS.</para>
                  </item>
               </list>
               <para>Note: This API has no effect on MAS builds.</para>
            </description>
         </method>
         <method name="setLoginItemSettings">
            <param name="settings" type="Object">
               <property optional="true" name="openAtLogin" type="Boolean"/>
               <property optional="true" name="openAsHidden" type="Boolean"/>
               <property optional="true" name="path" type="String"/>
               <property optional="true" name="args" type="String" isArray="true"/>
            </param>
            <description>
               <para>Set the app's login item settings.</para>
               <para>To work with Electron's autoUpdater on Windows, which uses Squirrel, you'll want to set the launch path to Update.exe, and pass arguments that specify your application name. For example:</para>
               <programlisting>...omitted...</programlisting>
               <para>Note: This API has no effect on MAS builds.</para>
            </description>
         </method>
         <method name="isAccessibilitySupportEnabled">
            <returns type="Boolean">
               <description>
                  <para>- true if Chrome's accessibility support is enabled, false otherwise. This API will return true if the use of assistive technologies, such as screen readers, has been detected. See https://www.chromium.org/developers/design-documents/accessibility for more details.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="setAboutPanelOptions">
            <param name="options" type="Object">
               <property optional="true" name="applicationName" type="String"/>
               <property optional="true" name="applicationVersion" type="String"/>
               <property optional="true" name="copyright" type="String"/>
               <property optional="true" name="credits" type="String"/>
               <property optional="true" name="version" type="String"/>
            </param>
            <description>
               <para>Set the about panel options. This will override the values defined in the app's .plist file. See the Apple docs for more details.</para>
            </description>
         </method>
         <method name="commandLine.appendSwitch">
            <param name="switch" type="String"/>
            <param optional="true" name="value" type="String"/>
            <description>
               <para>Append a switch (with optional value) to Chromium's command line.</para>
               <para>Note: This will not affect process.argv, and is mainly used by developers to control some low-level Chromium behaviors.</para>
            </description>
         </method>
         <method name="commandLine.appendArgument">
            <param name="value" type="String"/>
            <description>
               <para>Append an argument to Chromium's command line. The argument will be quoted correctly.</para>
               <para>Note: This will not affect process.argv.</para>
            </description>
         </method>
         <method name="dock.bounce">
            <param optional="true" name="type" type="String"/>
            <returns type="Integer">
               <description>
                  <para>an ID representing the request.</para>
               </description>
            </returns>
            <description>
               <para>When critical is passed, the dock icon will bounce until either the application becomes active or the request is canceled.</para>
               <para>When informational is passed, the dock icon will bounce for one second. However, the request remains active until either the application becomes active or the request is canceled.</para>
            </description>
         </method>
         <method name="dock.cancelBounce">
            <param name="id" type="Integer"/>
            <description>
               <para>Cancel the bounce of id.</para>
            </description>
         </method>
         <method name="dock.downloadFinished">
            <param name="filePath" type="String"/>
            <description>
               <para>Bounces the Downloads stack if the filePath is inside the Downloads folder.</para>
            </description>
         </method>
         <method name="dock.setBadge">
            <param name="text" type="String"/>
            <description>
               <para>Sets the string to be displayed in the dock’s badging area.</para>
            </description>
         </method>
         <method name="dock.getBadge">
            <returns type="String">
               <description>
                  <para>- The badge string of the dock.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="dock.hide">
            <description>
               <para>Hides the dock icon.</para>
            </description>
         </method>
         <method name="dock.show">
            <description>
               <para>Shows the dock icon.</para>
            </description>
         </method>
         <method name="dock.isVisible">
            <returns type="Boolean">
               <description>
                  <para>- Whether the dock icon is visible. The app.dock.show() call is asynchronous so this method might not return true immediately after that call.</para>
               </description>
            </returns>
            <description/>
         </method>
         <method name="dock.setMenu">
            <param name="menu" type="Menu"/>
            <description>
               <para>Sets the application's dock menu.</para>
            </description>
         </method>
         <method name="dock.setIcon">
            <param name="image" type="union">
               <option type="NativeImage"/>
               <option type="String"/>
            </param>
            <description>
               <para>Sets the image associated with this dock icon.</para>
            </description>
         </method>
      </methods>
   </object>
</file>
